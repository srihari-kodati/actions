name: ASCII Script Workflow

on:
   push

jobs:
  script:
  runs-on: ubuntu-latest

  steps:
  - name: List Repo Files
    run: ls -ltra

  - name: Executing Shell Script
    run: |
     chmod +x ascii-script.sh
     .ascii-script.sh